#!/usr/bin/env node

import("../dist/index.js")
  .then((module) => {
    module.main(process.argv.slice(2));
  })
  .catch((error) => {
    console.error("Failed to start ai-translate:", error);
    process.exit(1);
  });
